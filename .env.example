# SKILLLOOP_* is preferred. Existing OFFERFORGE_* settings remain supported.
SKILLLOOP_AUTH_ENABLED=true
SKILLLOOP_AUTH_USERNAME=change-me
SKILLLOOP_AUTH_PASSWORD=change-me

# The legacy filename is intentional so a rename does not create an empty database.
SKILLLOOP_DATABASE_PATH=./data/offerforge.db
