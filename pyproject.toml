[project]
name = "offerforge"
version = "0.1.0"
description = "Local-first web application for adaptive SDET interview practice."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra --basetemp=.pytest-tmp"
